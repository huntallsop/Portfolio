# Portfolio

This project contains Hunter May's product design portfolio, now implemented as a React single-page application powered by Vite.

## Getting started

```bash
npm install
npm run dev
```

The site will be available at the local address Vite prints in the console. To produce a production build, run `npm run build`.
